# JaT Ingestion Workers
Workers consume quarantined object-storage references, never browser paths or host paths. Parsed content is untrusted data and must not be treated as instructions. Parser/container isolation and queue transport are intentionally pending infrastructure decisions.
