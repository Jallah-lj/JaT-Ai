# JaT RAG — Current Implementation Boundary

Phase 3 has begun with governed metadata schema, provider-neutral retrieval contracts, deterministic embedding test fixture, and ingestion-worker contracts. Knowledge-base CRUD and document-registration APIs are the next implementation gate. No uploaded document content is parsed or exposed to a model until organization ownership, source/license metadata, quarantine, and asynchronous worker processing are implemented.
